# API base URL
# In development: /api (proxied by Vite to http://localhost:8080)
# In production: /api (proxied by nginx to backend)
# If backend is on a different domain: https://api.yourdomain.com/api
VITE_API_URL=/api
